<div class="lists-index">
	<h2>My Lists</h2>

	<% @lists.each do |list| %>
		<p><%= link_to list.name, list_path(list) %>
		<%= link_to 'Edit', edit_list_path(list) %>
		<%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' } %></p>
	<% end %>
</div>




